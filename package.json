{
  "name": "obaniwa-michael-portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "vercel-build": "vite build",
    "audit:lh": "npx lhci autorun",
    "audit:axe": "node scripts/axe-run.mjs",
    "audit:all": "npm run audit:lh && npm run audit:axe"
  },
  "devDependencies": {
    "@axe-core/cli": "^4.10.2",
    "@lhci/cli": "^0.13.0",
    "terser": "^5.44.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "pdfjs-dist": "^5.4.54",
    "three": "^0.180.0"
  }
}
